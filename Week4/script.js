function showMessage() {
    //do stuff
    //1) See the value of the input
    //look in document for element
    let input = document.getElementById("name-input")
    console.log(input.value)
    //2) Show a message with that name
}